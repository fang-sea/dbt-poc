version: 2

sources:
    - name: poc_raw_data
      database: open_test
      schema: raw_data
      loader: dms
      loaded_at_field: _loaded_at

tables:
    - name: orders
      identifier: Orders_
      loaded_at_field: updated_at #override source defaults
      columns:
        - name: id
          tests:
            - unique

        - name: price_in_usd
          tests:
            - not null
    - name: customers
    - name: payment
            
